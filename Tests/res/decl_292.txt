syn_fn_def(
  name: fn_symbol(or),
  params: [
    (
      type: seq_type(nonempty: true, elem_type: type_ref(type_symbol(boolexpr))),
      var: var(es)
    )
  ],
  local_fns: [],
  expr: tag_obj_expr(obj: const_or_var(es), tag: object(or)),
  res_type: type_ref(type_symbol(boolexpr))
)
