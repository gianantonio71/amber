syn_fn_def(
  name: fn_symbol(maybe_op),
  params: [
    (type: type_ref(type_symbol(instr)), var: var(instr)),
    (type: type_ref(type_symbol(bool)), var: var(cond))
  ],
  local_fns: [],
  expr: if_expr(
    else: const_or_var(no_op),
    branches: [(expr: const_or_var(instr), cond: const_or_var(cond))]
  ),
  res_type: type_ref(type_symbol(instr))
)
