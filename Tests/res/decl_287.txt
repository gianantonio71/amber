syn_fn_def(
  name: fn_symbol(get_curr_obj),
  params: [
    (
      type: union_type(
        {
          type_ref(type_symbol(setitvar)),
          type_ref(type_symbol(seqitvar))
        }
      ),
      var: var(it)
    )
  ],
  local_fns: [],
  expr: tag_obj_expr(obj: const_or_var(it), tag: object(get_curr_obj)),
  res_type: type_ref(type_symbol(natobjop))
)
