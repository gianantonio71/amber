syn_fn_def(
  name: fn_symbol(is_space),
  params: [(type: type_ref(type_symbol(int)), var: var(ch))],
  local_fns: [],
  expr: or(
    left: eq(left: const_or_var(ch), right: const_or_var(ascii_space)),
    right: eq(left: const_or_var(ch), right: const_or_var(ascii_newline))
  ),
  res_type: type_ref(type_symbol(bool))
)
