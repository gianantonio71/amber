syn_fn_def(
  name: fn_symbol(are_disjoint),
  params: [
    (type: type_ref(type_symbol(objpartset)), var: var(ps1)),
    (type: type_ref(type_symbol(objpartset)), var: var(ps2))
  ],
  local_fns: [],
  expr: not(
    ex_qual(
      sel_exprs: [
        not(
          fn_call(
            name: fn_symbol(are_disjoint),
            params: [const_or_var(ps2), const_or_var(p)],
            named_params: []
          )
        )
      ],
      source: [
        in_clause(src: const_or_var(ps1), ptrn: var_ptrn(name: var(p)))
      ]
    )
  ),
  res_type: type_ref(type_symbol(bool))
)
