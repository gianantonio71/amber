syn_fn_def(
  name: fn_symbol(some),
  params: [
    (
      type: set_type(nonempty: true, elem_type: type_ref(type_symbol(bool))),
      var: var(bs)
    )
  ],
  local_fns: [],
  expr: ex_qual(
    sel_exprs: [],
    source: [
      in_clause(src: const_or_var(bs), ptrn: obj_ptrn(object(true)))
    ]
  ),
  res_type: type_ref(type_symbol(bool))
)
